Built Svelte assets are generated by `./scripts/build-webdashboard.sh` (Docker).

Until you build, the legacy Alpine dashboard in this folder is still used.

Static images in `img/` are kept across rebuilds.

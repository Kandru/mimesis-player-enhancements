using Xunit;

namespace MimesisPlayerEnhancement.Tests.Infrastructure
{
    [CollectionDefinition(nameof(MimicPossessionSessionsCollection))]
    public sealed class MimicPossessionSessionsCollection
    {
    }
}
